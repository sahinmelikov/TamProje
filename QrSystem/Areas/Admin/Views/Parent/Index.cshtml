@model IEnumerable<ParentCategory>
<div class="container">
    <a class="btn btn-primary" asp-action="Create">Create</a>
    <table class="table table-bordered table-responsive">
        <thead>
            <tr>

                <th>Id</th>
                <th>Name</th>
                <th>IsActive</th>
                <th>CreateTime</th>
                <th colspan="2">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in (Model ?? new List<ParentCategory>()))
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.Name</td>
                    <th>@item.Isactive</th>
                    <td>@item.DateTime</td>
                    <td><a class="btn btn-warning" asp-action="Update" asp-route-id="@item.Id">Update</a></td>
                    <td><a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a></td>
                </tr>
            }
        </tbody>
    </table>
</div>